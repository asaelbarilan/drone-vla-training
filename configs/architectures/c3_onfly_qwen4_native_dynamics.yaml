# Matched C3 ablation for the OnFly native-dynamics causal check.
# It uses the identical decision agent, verifier, SUPER planner, model, and
# paper-reported motion envelope as C5, while intentionally omitting the slow
# monitor and its recovery authority.

_base_: c3_onfly_qwen4.yaml

id: c3_onfly_qwen4_native_dynamics
profile_of: c3
name: "OnFly decision+verifier + Qwen3-VL 4B, paper-native dynamics"
description: "Matched native-dynamics OnFly ablation without monitor or monitor memory."
tags: [onfly, decision_ablation, native_dynamics_profile, real_model, qwen3_vl_4b]

controller:
  params:
    max_yaw_rate_rps: 0.4

planner:
  params:
    max_acc_mps2: 0.6
