# Mixed-model smoke test only

This directory is not the controlled architecture comparison. It used different
foundation models for different architecture ports and must not be presented as
evidence that one architecture outperforms another.

Use `reports/professor_end_maps_shared_qwen4_controlled_20260831/` for the
corrected single-model comparison.
